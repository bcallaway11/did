# edid-covariates.R
# STUB ONLY: covariate path for EDiD is not yet implemented.
# All functions in this file stop with a clear message.

#' @keywords internal
estimate_outcome_regression_edid <- function(...) {
  stop("covariate path not yet implemented in edid(). ",
       "Pass covariates = NULL or omit the covariates argument.")
}

#' @keywords internal
build_polynomial_sieve_basis_edid <- function(...) {
  stop("covariate path not yet implemented in edid(). ",
       "Pass covariates = NULL or omit the covariates argument.")
}

#' @keywords internal
estimate_propensity_ratio_sieve_edid <- function(...) {
  stop("covariate path not yet implemented in edid(). ",
       "Pass covariates = NULL or omit the covariates argument.")
}

#' @keywords internal
estimate_inverse_propensity_sieve_edid <- function(...) {
  stop("covariate path not yet implemented in edid(). ",
       "Pass covariates = NULL or omit the covariates argument.")
}

#' @keywords internal
compute_generated_outcomes_cov_edid <- function(...) {
  stop("covariate path not yet implemented in edid(). ",
       "Pass covariates = NULL or omit the covariates argument.")
}

#' @keywords internal
compute_omega_star_conditional_edid <- function(...) {
  stop("covariate path not yet implemented in edid(). ",
       "Pass covariates = NULL or omit the covariates argument.")
}

#' @keywords internal
compute_per_unit_weights_edid <- function(...) {
  stop("covariate path not yet implemented in edid(). ",
       "Pass covariates = NULL or omit the covariates argument.")
}

#' @keywords internal
compute_eif_cov_edid <- function(...) {
  stop("covariate path not yet implemented in edid(). ",
       "Pass covariates = NULL or omit the covariates argument.")
}

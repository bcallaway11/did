% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/edid-utils.R
\name{active_mask_nocov_edid}
\alias{active_mask_nocov_edid}
\title{Active-unit mask for a no-covariate (g, t) cell's weighted Omega*}
\usage{
active_mask_nocov_edid(target_g, pairs, panel_obj)
}
\arguments{
\item{target_g}{scalar cohort value}

\item{pairs}{data.frame with column \code{gp} (the comparison cohorts), H rows}

\item{panel_obj}{panel object from \code{prepare_edid_panel()}}
}
\value{
logical vector length \code{panel_obj$n}
}
\description{
The nonzero rows of \eqn{\Psi} (\code{compute_psi_moments_nocov_edid()}) -- the
units that actually enter the cell's \eqn{\widehat\Omega^*} -- are the treated
cohort \eqn{g}, the never-treated group, and every comparison cohort
\eqn{g'_j} appearing in \code{pairs$gp}. Returns their union as a logical mask
over all units, for \code{\link{n_eff_edid}}.
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mboot.R
\name{mboot}
\alias{mboot}
\title{Multiplier Bootstrap}
\usage{
mboot(inf.func, DIDparams, pl = FALSE, cores = 1, return_V = TRUE)
}
\arguments{
\item{inf.func}{an influence function}

\item{DIDparams}{DIDparams object}

\item{pl}{whether or not to use parallel processing in the multiplier
bootstrap, default=FALSE.  Parallel processing only engages when the
influence function has more than 2500 rows and \code{cores} is greater than 1;
it is not available on Windows (a warning is issued and the bootstrap runs
sequentially there).}

\item{cores}{the number of cores to use with parallel processing,
default=1.  This only has an effect together with \code{pl=TRUE}; see \code{pl}.}

\item{return_V}{whether to compute and return the bootstrap variance
matrix \code{V}. Default is \code{TRUE}. Internal callers that only consume
\code{bres}, \code{se}, or \code{crit.val} set this to \code{FALSE} to skip the computation
(it is the only O(biters x k^2) step in the function).}
}
\value{
list with elements
\item{bres}{results from each bootstrap iteration}
\item{V}{variance matrix (\code{NULL} when \code{return_V = FALSE})}
\item{se}{standard errors}
\item{crit.val}{a critical value for computing uniform confidence bands}
}
\description{
A function to take an influence function and use the
multiplier bootstrap to compute standard errors and critical values for
uniform confidence bands.
}

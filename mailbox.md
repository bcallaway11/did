# Mailbox

---

## [2026-04-13] Planner — Handoff Notes for PT-All Pair Enumeration Fix

### For Builder (spec.md)

Implement the changes described in `spec.md`. Two files change: `R/edid-pairs.R` and `R/edid-nocov.R`. No other files need to change.

In `R/edid-pairs.R`, replace the PT-All loop (Section 2.2): the new loop iterates over `treatment_groups` only (never over `never_treated_val`). The tpre inclusion rule differs by whether `gp == target_g` (includes `period_1`) or `gp != target_g` (excludes `period_1`). Also update the roxygen `@details` paragraph (Section 2.3).

In `R/edid-nocov.R`, remove five dead `else` branches that handled `gp = Inf` in PT-All context: the `delta_gp_cache` builder loop (Section 3.1), two `n_gp` inline lookups (Section 3.2), the generated outcomes `if/else` (Section 3.4), and the EIF comparison-cohort `if/else` (Section 3.5). The PT-Post branches in all three functions are untouched.

Section 4 of spec.md explains why the degenerate pair `(gp=target_g, tpre=period_1)` needs no special handling — the zero vector flows through the existing formulas correctly. Section 6 documents edge cases for single cohort, anticipation>0, and last_cohort control group.

### For Tester (test-spec.md)

Verify the behaviors documented in `test-spec.md`. The critical behavioral contracts are: (1) no `gp=Inf` rows in PT-All pairs ever; (2) `(gp=target_g, tpre=period_1)` row is present when valid; (3) `(gp!=target_g, tpre=period_1)` rows are absent; (4) PT-Post output is byte-identical to pre-fix.

Unit tests U1–U8 cover the enumeration function directly with explicit expected row counts and content. Integration tests I1–I7 require a synthetic balanced panel. Edge cases E1–E3 cover single-cohort, early-cohort-no-cross-pairs, and degenerate Omega entries. Regression scenario R2 provides a programmatic check for absence of Inf gp values across all cells.

The author's reference implementation at `benchmark/edid_sim_original.R` is the ground truth for ATT/SE comparison in scenarios I5 and I6 — use it as a black-box oracle.

---

## [2026-04-13] Builder — Implementation Complete

### Changes delivered

- `R/edid-pairs.R`: PT-All loop now iterates over `treatment_groups` only; self-pair rule includes `period_1`; cross-pair rule excludes it. Roxygen `@details` updated.
- `R/edid-nocov.R`: Removed all five dead `gp=Inf` else branches in PT-All context (delta_gp_cache loop, n_gp_j, n_gp_k, generated outcomes, EIF comparison cohort). PT-Post paths untouched.

### Interface changes

None. All function signatures are unchanged. `enumerate_valid_pairs_edid()` return type (data.frame with columns `gp`, `tpre`) is unchanged; the only difference is that `gp` column now never contains `Inf` for `pt_assumption="all"`.

### Verification summary

- Pair count for benchmark data cell (g=3): 13 (was redundant due to 9 Inf pairs).
- ATT matches author's reference to < 1e-15.
- SE discrepancy ~0.00016 is pre-existing n vs n-1 design choice, not a regression.

### Notes for tester

- The `is.finite()` guards on Term C_j/C_k in the Omega inner loop are now vacuously satisfied but were not removed (harmless, low risk).
- Degenerate pair `(gp=3, tpre=0)` in the benchmark data: `delta_gp` is the zero vector; Omega entry is zero for that row/col; EIF contribution is zero. This is the expected behavior.

---
## BLOCK from tester (2026-04-13)

**Scenarios failing**: I1, I8, and marginally I2 (cell g=3,t=6)

**Root cause**: The Python `py_edid_all_gt.csv` benchmark was generated by the `diff-diff` Python library (v3.0.2), which uses a DIFFERENT moment set than the R `edid()` after the fix. The Python library:
- Exhibits near-singular Omega matrices (condition numbers 10^17 to 10^28)
- Falls back to pseudoinverse for all 15 post-treatment cells
- Produces ATT values up to 2.2% away from the R implementation

The R `edid()` instead:
- Matches the author's R reference `efficient_did_unc_stagg` to machine precision (max diff = 2.22e-15)
- Has well-conditioned Omega matrices (condition number ≈ 70 for benchmark cell g=3,t=3)
- Uses exactly 13 pairs per cell, no gp=Inf, period_1 included for self-pairs

**Action required from planner**: Revise test-spec.md acceptance criteria for Scenarios I1 and I8:
- Replace "Python `py_edid_all_gt.csv`" as ground truth with the author's R reference values
- Update tolerances to match the machine-precision agreement with the author's code (1e-10 vs author R reference, which the implementation already achieves)
- OR: regenerate the Python benchmark with a library version that uses the same 13-pair moment set as the fixed R edid()

**All other tests pass**: 1041 PASS, 0 FAIL in the full package test suite.

---

## [2026-04-17] Builder — Covariate path wiring complete

### Interface changes

- `edid()` now accepts a new argument `xformla = NULL` (one-sided formula). The old `covariates` argument now errors with a redirect message instead of silently accepting NULL.
- `edid_fit` object now includes `xformla` field.
- `panel_obj` now includes `covariate_matrix` (n×d matrix or NULL) and `xformla` fields.
- `fit_edid_cells` signature changed from `covariates` to `xformla`; any direct callers must update.
- `prepare_edid_panel` signature: `xformla = NULL` added before `covariates`.
- `validate_edid_inputs` signature: `xformla = NULL` added before `covariates`.

### Bug fixed in edid-cov.R
`build_basis_matrix_edid` now uses a fallback sentinel `list(fallback=TRUE, ...)` instead of `NULL` when the B-spline fails, to avoid list-slot deletion.

### Smoke test passed
No-covariate and covariate paths both return finite ATT estimates. B-spline fallback warnings are expected for small n.

---

## [2026-04-17] Tester — Covariate Path Validation Complete

**Overall verdict**: PASS after 3 bug fixes applied during testing.

### Bugs Fixed

**BUG-1** (`R/edid-cov.R`): `build_basis_matrix_edid` — `<<-` inside `tryCatch` failed to assign to function-local `bs_objects` list, causing B-spline to always fall back to linear basis. Fixed by storing `tryCatch` result then assigning directly.

**BUG-2** (`R/edid-cov-eif.R`): `compute_generated_outcomes_cov_edid` — Spurious `+ m_diff` in generated outcome formula. `E[phi] = ATT + E[m_diff]` instead of `ATT`. For DGPs with non-zero unconditional outcome trends (e.g., DGP5), this caused ~1 unit of bias. Fixed by removing `+ m_diff` from both PT-Post and PT-All branches.

**BUG-3** (`R/edid-cov-eif.R`, `R/edid-fit.R`): Self-comparison pairs (`gp = target_g`) used the TREATED group as its own control (`Igp = I(G=g)`, `r = P(G=g|X)/P(G=g|X) = 1`). This caused ATT estimates to collapse near zero. No-covariate path correctly uses never-treated as the DiD comparison for these pairs. Fixed by remapping `gp = g → gp = Inf` in `pairs_for_nuisance` before calling nuisance estimators, and adding `gp_lookup = Inf` in `compute_generated_outcomes_cov_edid`.

### Test Results (after fixes)

| Category | Verdict |
|----------|---------|
| 1. Load check | PASS |
| 2. Regression test xformla=~1 | PASS (exact 0 diff) |
| 3. Output structure | PASS |
| 4. Validation errors | PASS |
| 5. Nuisance unit tests | PASS |
| 6. DGP5 simulation benchmark (R=50, n=200) | PASS (ATT bias < 0.10; SE ratio marginal for ATT(3,4)) |

### Known Limitations

- SE ratio for ATT(3,4) = 1.65 (threshold 1.5) — conservative inference, not anti-conservative; spec notes threshold is loose.
- `validate_edid_inputs` does not check NA covariates or time-varying covariates (test-spec.md 3.5 rows 3-4).

Full evidence in `/Users/marcelortiz/Library/CloudStorage/OneDrive-Emory/GitHub Repositories/did/audit_cov.md`.
